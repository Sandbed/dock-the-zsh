{
  "name": "${LANGUAGE} Development Environment",
  "dockerFile": "Dockerfile",
  
  "remoteUser": "codemaxing",
  
  "overrideCommand": true,
  "postStartCommand": "sleep infinity",
  
  "settings": {
    // Language-specific settings go here
  },
  
  "extensions": [
    // Language-specific extensions go here
  ],
  
  "forwardPorts": [8080, 3000, 5000, 8000],
  
  "postCreateCommand": "${POST_CREATE_COMMAND}"
}